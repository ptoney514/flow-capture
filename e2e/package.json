{
  "name": "screen-shots-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "E2E tests for flow capture tool and web API",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:capture": "playwright test capture-tool/",
    "test:api": "playwright test web-api/"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
